# Cloudflare Pages 部署与开发命令

## 1. 登录 Cloudflare
npx wrangler login

## 2. 本地开发/预览
# 使用 wrangler.toml 中的变量
npx wrangler pages dev public

## 3. 部署到云端
# 部署 public 目录到名为 cfip 的项目
npx wrangler pages deploy public --project-name cfip

npx wrangler pages deploy public --project-name cfip --branch main


## 4. 删除项目 (慎用)
npx wrangler pages project delete cfip
